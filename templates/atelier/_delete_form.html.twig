<form method="post" action="{{ path('atelier_delete', {'id': atelier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atelier.id) }}">
    <button class="btnDelete"><i class="fas fa-trash"></i></button>
    {# <a class="lienEdit" href="{{ path('atelier_delete', {'id': atelier.id}) }}"><i class="fas fa-trash"></i></a> #}
</form>
